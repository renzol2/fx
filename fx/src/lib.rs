pub mod biquad;

pub const DEFAULT_SAMPLE_RATE: usize = 44_100;

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
